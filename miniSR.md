# 实验原则
1. 在后台运行，不依赖于ssh连接
2. 必须清楚训练和推理的维度数据流，包括输入、输出、中间层等
3. 从倒序开始使用显卡，优先使用显卡 7，再使用显卡 6，以此类推
4. 先复现别人的实验结果，再尝试自己的实验
5. 使用 lab 文件夹下的基础设施进行实验，尽量少的创建新的文件或目录，以添加代码为主

# 评估指标
每一个检查点的模型都要在测试集中进行评估，记录下 PSNR、SSIM、LPIPS、DISTS 等指标，并且留下样图
PSNR（峰值信噪比）
基于 MSE 计算，是保真度的首要数值指标。数值高通常代表像素还原精准，但与人类感知不完全一致。

SSIM / MS-SSIM（结构相似性）
综合亮度、对比度和结构信息。MS-SSIM 多尺度计算，与人眼视觉一致性优于 PSNR。

LPIPS（学习感知图像块相似度）
使用深度网络（如 AlexNet/VGG/SqueezeNet）特征差异，与主观感知高度相关，是感知质量评价的核心指标。

DISTS（深度图像结构与纹理相似度）
同时敏感于结构失真和纹理变化，比 LPIPS 更全面。

# 模型架构

原图插值放大到 x2 倍大小 -> 切分成多个 512*512 块的小图像

对于每一块小图像 512*512，先保存图像命名为 图像 0-> 做 3*3 步长为 1 的卷积 3 通道 -> Relu -> 3*3 步长为 1卷积拓宽通道将输入通道数从 3 增加到 320 保存命名为图像 1-> 做 ReLU激活函数 -> 做 3*3 卷积 步长为 1 320 通道 再加上图像 1-> 做 3*3 卷积步长为 2 通道为 640，保存图片命名为图像 2 -> 做 ReLu激活函数 -> 做 3*3 卷积步长为 1 通道为 640。再加上图像 2 -> 做 3*3 卷积步长为 2 通道为 1280，保存图片命名为图像 3 -> 做 ReLu激活函数 -> 做 3*3 卷积步长为 1 通道为 1280 -> 再做 ReLu 激活函数并加上图像 3 -> 插值放大使得 H、W 乘 2 -> 3*3 步长为 1 通道为 640 -> ReLu ->  3*3 步长为 1 通道为 640 再加上图像  2 -> 插值放大使得 H、W 乘 2 -> 3*3 步长为 1 通道为 320 -> ReLu -> 3*3 步长为 1 通道为 320 再加上图像1 -> 3*3 步长为 1 的卷积 3 通道 再加上 图像 0 -> 得到最终图像

最后将所有小切片拼接起来，重叠的地方使用加权平均合并


注： 所有架构都要使用 0 权重开始训练，而不是使用随机权重

# 损失函数计算小波损失 (Wavelet Loss)

对 \(I_{SR}\) 和 \(I_{HR}\) 做相同的小波分解，然后在各个高频子带（甚至包括低频子带）上计算重建损失：
\[
\mathcal{L}_{\text{wavelet}} = \sum_{s} \lambda_s \left\| DWT_s(I_{SR}) - DWT_s(I_{HR}) \right\|_1
\]
其中 \(s\) 遍历 LL、LH、HL、HH 各子带，\(\lambda_s\) 是各子带的权重（通常给高频子带更大权重）。

# 下一步改进
1. 在训练的时候，给输入的图片添加轻微高斯噪声，让模型学习轻微去噪的能力，获得部分的生成能力
2. 使用 transformer 架构来做高频恢复问题
3. 编码器-解码器架构?
4. 添加像素缺失的数据增强任务，让图片学一下补全的任务